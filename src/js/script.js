window.onload = (event) => {
  window.scrollTo({
    top: 0,
    left: 0,
    behavior: 'smooth',
  });
  // const item_list = document.querySelector('.item_list');
  // const item_opts_container = document.querySelector('.item-opts-container');
  // document.querySelectorAll('.item img').forEach((item) => {
  //   item.addEventListener('click', (event) => {
  //     item_list.classList.add('shift');
  //     item_opts_container.classList.add('show');
  //   });
  // });
};
